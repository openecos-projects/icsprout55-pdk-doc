version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
    nodejs: "22"
  apt_packages:
    - pngquant
    - jpegoptim
  jobs:
    build:
      html:
        - cd doc && MKDOCS_ENV=deploy make setup && make gen-css && make build-doc
