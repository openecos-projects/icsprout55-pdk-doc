version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  apt_packages:
    - pngquant
    - jpegoptim
  jobs:
    build:
      html:
        - cd doc && MKDOCS_ENV=deploy make setup && make build-rtd && make build-rtd MKDOCS_LANG=en
